

import React from 'react';
import {StyleSheet, View} from 'react-native';

export interface CustomKeyboardAvoidViewProps{
    
}

const CustomKeyboardAvoidView = (() => {
    
    const styles = StyleSheet.create({
        root: {
            
        },
    });
    
    const CustomKeyboardAvoidView = (props: CustomKeyboardAvoidViewProps) => {
        return <View style={styles.root}>
            
        </View>
    }
    return CustomKeyboardAvoidView;
})();

export default CustomKeyboardAvoidView;






