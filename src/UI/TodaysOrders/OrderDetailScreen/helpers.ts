import { Color } from '../../../helpers/colors';

export const OrderDetailScreenConstants = {
  headingFontSize: 18,
  regularFontSize: 16,
  titleColor: Color.gray(0.3).stringValue,
  subtitleColor: Color.gray(0.7).stringValue,
};
